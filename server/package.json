{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build" : "cd ../client && npm run build",
    "install-client": "cd ../client && npm install",
    "install-server": "npm install",
    "heroku-postbuild" : "npm install-server && npm run build && npm run install-client",
    "start:dev": "nodemon server.js",
    "start:prod": "SET NODE_ENV=production& nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.5.0",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.19"
  }
}
